services:
  reminder-bot:
    build: .
    env_file: .env
    environment:
      DB_PATH: /app/data/reminders.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    stop_grace_period: 30s
