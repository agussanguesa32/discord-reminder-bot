# Root .gitignore already covers node_modules, .next, logs, .env*, OS files.
# This file exists as a safety net for tools that only look at the nearest .gitignore.

/node_modules
/.next/
/out/
/.vercel/
.env*
*.tsbuildinfo
next-env.d.ts
/coverage
npm-debug.log*
yarn-debug.log*
